const name = "hello";

function shortcut(string) {
  const vowels = ["a", "e", "i", "o", "u"];
  for (let v = 0; length = ; v++) {
    const newWord = string.replace(v, "");
    console.log(newWord);
  }
  return string;
}
//vowels.forEach((vowel) => {
//const word = string.replace(vowel, "");
//});

shortcut("hello");
// hey
//hello.
